#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../autoless/bin/autoless" "$@"
  ret=$?
else 
  node  "$basedir/../autoless/bin/autoless" "$@"
  ret=$?
fi
exit $ret
